pipeline {
    agent any

    stages {
        stage('pull from git') {
            steps {
                sh 'echo "hello word"'
            }
        }
        stage('docker build') {  
            steps {
                sh 'echo "yaniv git"'
            }
        }
    }
}
